# LiteLLM 的 OpenAI-compat 端点 + key（用户填实际值）。openrouter recipe 嵌入认 OPENROUTER_BASE_URL
OPENROUTER_BASE_URL=https://llm.aigallerie.com/v1    # LiteLLM /v1（OpenAI-compat embeddings）
OPENROUTER_API_KEY=sk-REPLACE_ME                     # LiteLLM key
