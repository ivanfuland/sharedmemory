[project]
name = "sharedmemory"
version = "0.0.1"
description = "M0 contract artifacts for the shared-memory layer"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
