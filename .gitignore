__pycache__/
*.pyc
.venv/
.pytest_cache/
# 沙盒运行产物（可重建，不进 git）
sandbox/
*.db-wal
*.db-shm
# 探测到的真实数据快照（可能含隐私，绝不进 git）
fixtures/real-snapshots/
# M1 secrets（密码/API key，绝不进 git）
infra/pg-memory/.env
infra/gbrain/config.env
infra/gbrain/clients.env
infra/distill/config.env*
infra/distill/audit.log
# distill 运行时产物（含真实会话 fact_text/源路径/MEMORY 分析，PUBLIC 仓绝不进 git）
infra/distill/bridge-state.db
infra/distill/bridge-state.db-*
infra/distill/memory-hygiene.md
infra/distill/review-queue/
sandbox/gbrain-pg/
.superpowers/
# M2 Task2 generated env（bash-sourced clean re-emit，含 secret，绝不进 git）
infra/gbrain/serve/env.generated
# M4 cass-mcp secrets（bearer token / 访问日志含 secret，绝不进 git）
infra/cass-mcp/*.env
infra/cass-mcp/*.log

# 喂料水位线运行时状态(含真实 ts,不进 PUBLIC 仓)
cass-corpus-feed/
*.watermark.json

# 本机私有 agent 指令(CLAUDE.local.md),永不进本 PUBLIC 仓
CLAUDE.local.md
infra/everos-mcp/*.env
